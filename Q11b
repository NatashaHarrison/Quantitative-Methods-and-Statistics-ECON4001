import pandas as pd
import matplotlib.pyplot as plt
import statsmodels.api as sm

mario = pd.read_csv("mariokart.csv")

X = sm.add_constant(mario["wheels"])
y = mario["total_pr"]

model = sm.OLS(y, X).fit()

sm.qqplot(model.resid, line='45')

plt.title("Q-Q Plot")

plt.show()
